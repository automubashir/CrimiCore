import PageShell from '@/components/layout/PageShell/PageShell'

export const metadata = {
  title: 'Settings | CrimePanel',
}

export default function SettingsPage() {
  return (
    <PageShell
      title="Settings"
      subtitle="Configure system preferences and account settings"
    />
  )
}
